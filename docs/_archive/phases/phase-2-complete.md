# Phase 2 complete

Merged to `main` after implementing:

- Project Linux-user isolation plans
- Multi-version Node.js + PHP
- MySQL/MariaDB + Redis plans
- Nginx reverse proxy + Let’s Encrypt
- Full three-axis RBAC
- Kernel Sandbox planner
- Offline / Protection Mode

Shipped modules live under `packages/core/src/hosting`, `security/rbac.ts`, `security/sandbox.ts`, `services/protection.ts` with unit tests.
