# npm publish

> Language: English | [中文](./npm-publish-ZH.md)

Publish `@ysk/*` packages from the monorepo after version bump and CI green.

```bash
pnpm -r build
# publish per package policy
```

Not required for self-hosted git deploy.
