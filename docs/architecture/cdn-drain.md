# cdn.ts drain status

Canonical workdir: `/home/ki/文件/ysk-server/`.

| Domain | Module | Wave |
|--------|--------|------|
| CDN nodes registry / probe / drain | `routes/cdn-nodes.ts` | **K3** |
| CDN sites / dashboard / SSL / DNS | `routes/cdn-sites.ts` | **K3** |

`routes/cdn.ts` thin dispatcher: `nodes → sites`.
