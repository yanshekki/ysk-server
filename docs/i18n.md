# Internationalization (i18n)

> Language: English | [中文](./i18n-ZH.md)

## Supported languages

### Tier-1 (quality bar — full native catalogs)

| Code | Notes |
|------|--------|
| **zh-HK** | Default UI — Hong Kong written Chinese |
| **zh-CN** | Simplified Chinese |
| **en** | English (key SSOT for multi-locale expansion) |

### Tier-2 (world top languages — scaffolded from English)

| Code | Notes | Direction |
|------|--------|-----------|
| **hi** | Hindi | LTR |
| **es** | Spanish | LTR |
| **ar** | Arabic | **RTL** |
| **fr** | French | LTR |
| **bn** | Bengali | LTR |
| **pt** | Portuguese | LTR |
| **id** | Indonesian | LTR |
| **ur** | Urdu | **RTL** |

`zh-TW` / bare `zh` normalize to `zh-HK`. Legacy `in` (Indonesian) → `id`.  
Registry: `packages/shared/locales/locales.json` and `packages/shared/src/i18n/normalize-locale.ts`.

## Source of truth

```
packages/shared/locales/{locale}/{namespace}.json
packages/shared/locales/locales.json          ← locale list + RTL + labels
```

Web: `t()` via react-i18next (`apps/web/src/shared/lib/i18n.ts`).  
Backend: `tl()` / `t(locale, key)`. CLI: `--locale` / `YSK_LOCALE`.  
RTL: `isRtlLocale()` sets `<html dir="rtl" lang="…">` for `ar` and `ur`.

About-tab guides: Tier-1 has `apps/web/src/shared/guides/data/{zh-HK,zh-CN,en}.json`; Tier-2 falls back to English.

## Developer workflow

1. **Tier-1 quality bar:** edit zh-HK / zh-CN / en namespaces in parallel.  
2. Prefer semantic keys; write zh-HK first, then zh-CN and en.  
3. Tier-2 starts as English copies under `packages/shared/locales/{hi,es,…}/`; native translation can replace strings later without key changes.  
4. `pnpm i18n:rebuild`  
5. `pnpm i18n:check-keys && pnpm i18n:check-glossary && pnpm i18n:check-ui && pnpm i18n:check-api && pnpm i18n:check-drift && pnpm i18n:check-guides`

## Gates

| Script | Role |
|--------|------|
| `i18n:check-keys` | Key sets match **all** locale dirs (SSOT = en) |
| `i18n:check-ui` | No hardcoded CJK in web sources |
| `i18n:check-api` | No hardcoded CJK operator strings in core/server |
| `i18n:check-glossary` | zh-HK forbidden TW/CN terms + spoken Cantonese |
| `i18n:check-drift` | translation.json must match namespace JSON (every locale) |
| `i18n:check-guides` | About-tab catalogs aligned for Tier-1 (zh-HK / zh-CN / en) |

Copy rules: [copy-i18n-standard.md](./copy-i18n-standard.md).

## Request locale

| Source | Priority |
|--------|----------|
| HTTP `Accept-Language` | Primary for API messages |
| `?locale=` | Override |
| User profile `locale` | After login |
| CLI `--locale` / env | CLI session |

## Related

[architecture/overview.md](./architecture/overview.md) · [frontend-ui.md](./frontend-ui.md) · [copy-i18n-standard.md](./copy-i18n-standard.md)
