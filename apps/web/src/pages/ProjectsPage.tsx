export function ProjectsPage() {
  return (
    <div className="card">
      <h1>Projects</h1>
      <p className="muted">
        Each project runs as an independent Linux user/group with isolated home directory,
        multi-version Node.js/PHP, and Nginx reverse proxy.
      </p>
    </div>
  );
}
