/**
 * System-wide page-level tabs — only allowed page tab primitive.
 * Thin rename of Tabs; prefer importing PageTabs in new code.
 */
export { Tabs as PageTabs, Tabs } from './Tabs';
export type { TabsProps as PageTabsProps, TabItem as PageTabItem, TabsProps, TabItem } from './Tabs';
